<?xml version="1.0" encoding="utf-8"?>
<!--
  AndroidManifest.xml — Cordite Wars: Six Fronts
  Version strings are kept in sync by bump-version.py.
  Minimum SDK: 24 (Android 7.0)  |  Target SDK: 35 (Android 15)
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.koshkikode.corditewars"
          android:versionCode="10000"
          android:versionName="1.0.0">

  <!-- ─── Permissions ────────────────────────────────────────────────── -->

  <!-- Multiplayer: internet and LAN discovery -->
  <uses-permission android:name="android.permission.INTERNET"/>
  <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
  <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

  <!-- Haptic feedback on unit selection and combat events -->
  <uses-permission android:name="android.permission.VIBRATE"/>

  <!-- ─── Feature requirements ────────────────────────────────────────── -->

  <!-- OpenGL ES 3.0 is the minimum for Compatibility renderer -->
  <uses-feature android:glEsVersion="0x00030000" android:required="true"/>

  <!-- Touch screen required for touch controls -->
  <uses-feature android:name="android.hardware.touchscreen" android:required="true"/>

  <!-- ─── Application ─────────────────────────────────────────────────── -->

  <application
      android:label="Cordite Wars: Six Fronts"
      android:icon="@mipmap/icon"
      android:allowBackup="false"
      android:hardwareAccelerated="true"
      android:requestLegacyExternalStorage="false"
      android:usesCleartextTraffic="false">

    <!-- Main game activity — Godot's GodotApp or GodotFragment host -->
    <activity
        android:name="com.godot.game.GodotApp"
        android:label="Cordite Wars: Six Fronts"
        android:theme="@style/GodotTheme.Splash"
        android:launchMode="singleTop"
        android:exported="true"
        android:screenOrientation="landscape"
        android:configChanges="orientation|keyboardHidden|screenSize|smallestScreenSize|screenLayout|density|uiMode"
        android:resizeableActivity="false">

      <!-- Intent filter so the launcher can find the game -->
      <intent-filter>
        <action android:name="android.intent.action.MAIN"/>
        <category android:name="android.intent.category.LAUNCHER"/>
      </intent-filter>

      <!-- Metadata consumed by Godot's Android bootstrap -->
      <meta-data android:name="org.godotengine.editor.version" android:value="4.6"/>
      <meta-data android:name="org.godotengine.game.MainPack" android:value="CorditeWars.pck"/>
    </activity>

  </application>

</manifest>
